// this object holds references to the views and scenes used in our custom code
// instead of hard-coding these views/scenes into our helper functions, we should place them here and import them to the functions
// that way, if the view/scenes changes, we only need to update them here

export default {
  api_page: {
    scene_key: "scene_143",
    edit_projections_grid_view_key: "view_232",
  },
};
